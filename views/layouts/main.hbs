<!DOCTYPE html>
<html lang="pt_br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My System</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap');

        * {
            box-sizing: border-box;
            list-style: none;
        }

        html {
            scroll-behavior: smooth;
            background: linear-gradient(150deg, black, rgb(0, 0, 141));
        }

        body {
            font-family: Ubuntu, sans-serif;
            margin: 0;
        }

        html,
        body {
            height: 100%;
            margin: 0;
            padding: 0;
        }

        .header {
            padding: 80px;
            text-align: center;
            color: white;
        }

        .header h1 {
            font-size: 40px;
        }

        .navbar {
            width: 50%;
            text-align: center;
            margin: 0 auto;
            padding: 60px;
            overflow: hidden;
        }

        .navbar a {
            color: white;
            text-align: center;
            padding: 14px 20px;
            text-decoration: none;
        }

        .navbar a:hover {
            color: blue;
            transition: 0.3s;
        }

        .row {
            display: flexbox;
            display: flex;
            flex-wrap: wrap;
        }

        .side {
            flex: 30%;
            padding: 20px;
        }

        .main {
            flex: 70%;
            padding: 20px;
        }

        .validacao {
            width: 50%;
            text-align: center;
            margin: 0 auto;
            padding: 60px;
        }

        .amostraIMG {
            height: 200px;
            background-color: burlywood;
            width: 100%;
            padding: 20px;
        }

        .amostraDeImg {
            height: 60px;
            background-color: burlywood;
            width: 100%;
            padding: 20px;
        }

        h2,
        h3,
        h5 {
            color: white;
            font-size: 20px;
        }

        p {
            color: white;
            font-size: 17px;
        }

        .rodape {
            text-align: center;
            color: white;
            margin: 8% 0;
        }

        .rodape h2 {
            text-align: right;
            margin: auto 0;
            font-size: 15px;
        }


        input {
            width: 50%;
            padding: 12px;
            border: 1px solid #223630;
            border-radius: 4px;
            box-sizing: border-box;
            display: block;
            margin: 10px auto;
        }

        label {
            color: white;
            display: block;
            margin: auto;
            width: 50%;
            font-weight: bold;
        }

        .btn {
            width: 50;
            height: 45px;
            background: white;
            border: none;
            outline: none;
            border-radius: 4px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 1);
            cursor: pointer;
            font-size: 14px;
            color: black;
            font-weight: 600;
        }

        .container {
            padding: 20px;
        }

        #message {
            display: none;
            background-color: white;
            color: rgb(29, 44, 29);
            position: relative;
            padding: 20px;
            margin-top: 10px;
        }

        #message p {
            color: white;
            padding: 10px 35px;
            font-size: 18px;
        }

        .valid {
            color: rgb(49, 190, 49);
        }

        .valid:before {
            position: relative;
            left: -35px;
            content: "✔️";
        }

        .invalid {
            color: rgb(190, 42, 42);
        }

        .invalid:before {
            position: relative;
            left: -35px;
            content: "❌";
        }

        @media screen and (max-width : 700px) {

            html,
            body {
                height: 100%;
                margin: 0;
                padding: 0;
            }

            .row {
                flex-direction: column;
            }
        }

        @media screen and (max-width : 400px) {
            .navbar a {
                float: none;
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <header class="header">
        <h1>Sistema de Login</h1>
        <h2>
            <strong> <!-- Testar se o usuário está logado -->
                {{#if msg}}
                <span>
                    Bem vindo, você está {{{msg}}}
                </span>
                {{else}}
                <span>
                    Faça o login para acessar o sistema.
                </span>
                {{/if}}
            </strong>
        </h2>
    </header>
    <div class="navbar">
        <nav>
            <a href="/">Home</a> |
            <a href="/login">Login</a> |
            <a href="/logout">Logout</a>
        </nav>
    </div>
    <div>
        {{{body}}}
    </div>
    <div class="rodape">
        <footer>
            <h2>contato: victor.anibal60@gmail.com</h2>
        </footer>
    </div>
</body>

</html>